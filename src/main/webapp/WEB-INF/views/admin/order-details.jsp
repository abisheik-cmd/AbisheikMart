<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %><%@ taglib prefix="fmt" uri="http://java.sun.com/jsp/jstl/fmt" %>
<c:set var="activeNav" value="admin" scope="request" /><jsp:include page="/WEB-INF/views/common/header.jsp" /><jsp:include page="/WEB-INF/views/common/navbar.jsp" />
<main class="main-content"><a href="${pageContext.request.contextPath}/admin/orders" style="color:var(--accent-cyan);">← Orders</a><c:choose><c:when test="${order != null}"><div class="modal-card" style="position:static;margin:1.5rem 0;width:min(850px,100%);"><h2 style="color:#fff;">Order #${order.id}</h2><p><strong>Buyer:</strong> ${order.userName} · ${order.userEmail}</p><p><strong>Delivery:</strong> ${order.deliveryAddress} · ${order.phoneNumber}</p><p><strong>Status:</strong> ${order.status} · <strong>Total:</strong> ₹<fmt:formatNumber value="${order.totalAmount}" pattern="#,##0.00" /></p><table class="data-table"><thead><tr><th>Product</th><th>Seller</th><th>Qty</th><th>Unit Price</th><th>Subtotal</th></tr></thead><tbody><c:forEach items="${order.items}" var="item"><tr><td>${item.productName}</td><td>${item.sellerId}</td><td>${item.quantity}</td><td>₹<fmt:formatNumber value="${item.unitPrice}" pattern="#,##0.00" /></td><td>₹<fmt:formatNumber value="${item.subtotal}" pattern="#,##0.00" /></td></tr></c:forEach></tbody></table><form method="post" action="${pageContext.request.contextPath}/admin/orders/status" style="display:flex;gap:.6rem;max-width:420px;margin-top:1rem;"><input type="hidden" name="orderId" value="${order.id}"><select class="form-control" name="status"><option value="PLACED" ${order.status eq 'PLACED' ? 'selected' : ''}>PLACED</option><option value="SHIPPED" ${order.status eq 'SHIPPED' ? 'selected' : ''}>SHIPPED</option><option value="DELIVERED" ${order.status eq 'DELIVERED' ? 'selected' : ''}>DELIVERED</option><option value="CANCELLED" ${order.status eq 'CANCELLED' ? 'selected' : ''}>CANCELLED</option></select><button class="btn btn-primary">Update Status</button></form></div></c:when><c:otherwise><p>Order not found.</p></c:otherwise></c:choose></main><jsp:include page="/WEB-INF/views/common/footer.jsp" />
